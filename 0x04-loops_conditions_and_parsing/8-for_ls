#!/usr/bin/env bash
# This script lists all files in the current directory, showing only the part after the first dash

for file in *; do
    # Skip hidden files
    [[ "$file" == .* ]] && continue
    # Extract and display everything after the first dash
    echo "${file#*-}"
done
