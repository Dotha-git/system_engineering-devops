#!/usr/bin/env bash
# Displays messages indefinitely and handles SIGTERM

# When SIGTERM is received, print the message
trap "echo I am invincible!!!" SIGTERM

# Infinite loop
while true
do
    echo "To infinity and beyond"
    sleep 2
done
