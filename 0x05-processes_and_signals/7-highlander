#!/usr/bin/env bash
# Script that prints a message indefinitely and handles SIGTERM

# Handle SIGTERM without exiting
trap 'echo "I am invincible!!!"' SIGTERM

while true; do
    echo "To infinity and beyond"
    sleep 2
done
