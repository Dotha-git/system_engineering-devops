#!/usr/bin/env bash
# Prints messages indefinitely and responds to SIGTERM without exiting

# Flush output immediately to be seen by checker
trap 'printf "I am invincible!!!\n" >&1' SIGTERM

while true; do
    printf "To infinity and beyond\n" >&1
    sleep 2
done
