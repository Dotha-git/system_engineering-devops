#!/usr/bin/env bash
# Script that prints a message indefinitely and does not exit on SIGTERM

# Trap SIGTERM and print a message instead of stopping
trap 'echo "I am invincible!!!"' SIGTERM

while true; do
    echo "To infinity and beyond"
    sleep 2
done
